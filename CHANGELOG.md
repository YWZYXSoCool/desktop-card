# 更新日志

本文件记录本项目的显著变更。格式遵循 [Keep a Changelog]，版本遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.0] - 2026-08-13

### 新增
- 新增外部 widget **B站下载**：输入 B 站链接（BV/av/URL），可下载 **视频（纯视频流）/ 视频音频（MP4 单文件）/ 音频（音频流）/ 封面 / 弹幕**。
- 支持**混合批量下载**：列表里每个条目可独立选择类型，一键「全部下载」按各自类型并行下载。
- 下载进度经宿主事件驱动实时刷新（沙箱内可逐项取消），完成后 toast 提示落盘路径。
- 平台新增通用 `download` 权限（后端零业务、不关心前端）：裸 HTTP、URL→文件、文本→文件、下载目录查询，任何需要网络/文件下载的外部 widget 都能复用；下载目录由 `download.dir` 设置项控制（默认用户下载文件夹）。
- 新增 **Widget 商店**：主页左上角新增商店按钮，点击打开独立窗口，从 GitHub 仓库拉取目录，可下载安装 / 卸载外部 widget。
- 商店支持自定义源仓库（`owner/repo[#branch]`，默认 `YWZYXSoCool/desktop-card-widgets`），安装前展示该 widget 声明的权限供确认。
- 支持**从本地文件夹导入 widget**：在「已安装」页选一个含 `widget.json` 的目录，整目录复制进 widget 根目录（已存在同名则提示先卸载）。
- 安装/卸载外部 widget 后**主窗口注册表热重载**，新 widget 立即可用，无需重启应用。
- **设置窗口放大**：默认尺寸由 320×200 增大到 400×460，设置项较多时窗口内滚动。
- **为全部 widget 补齐设置项**（每项均为卡片上没有的配置，不重复卡片已有控件）：
  - 音频播放器：启动后自动续播、显示歌词。
  - 剪贴板：显示图片条目、显示文件条目。
  - 颜色取色器：取色后自动复制格式（HEX/RGB/HSL/不复制）。
  - 倒计时：到点提醒开关。
  - 待办：显示已完成、已完成置底。
  - 单位转换器：结果小数位（自动/0–6 位）。
- **音频播放器升级为播放列表播放器**：支持一次拖入多首歌曲组成列表，自动连播，重启后恢复列表与当前位置。
- 播放器新增**上一首 / 下一首**、**随机播放**（洗牌序不重复，可回退）、**循环模式**（列表循环 / 单曲循环 / 关闭）与**播放速度**（0.5x–2x 循环切换）。
- 播放列表页（卡片内「列表」按钮进入）：可点切歌、移除单曲、清空列表，空列表时提示拖入音频。
- 平台 `onDrop` 契约升级：由单个路径改为 `paths: string[]`，支持一次拖入多份文件（影响宿主 / 沙箱 / 类型定义）。

## [0.2.0] - 2026-08-13

### 新增
- 主页左下角显示应用版本号，有更新时在旁边显示更新图标，点击即可下载安装。
- 新增「倒计时」widget：设置目标日期与名称，常驻显示剩余天数/时分秒，到点提醒。
- 英英词典 AI 来源扩充：新增 OpenAI / 智谱 GLM / Kimi / 通义千问 / OpenRouter，并支持完全自定义来源（手动填接口地址 + 模型 + Key）。
- 英英词典搜索升级：输入时按历史搜索 + 收藏实时给出模糊建议，支持收藏单词（收藏优先排序），回车/方向键可快速选择。
- widget 搜索升级：同样按 收藏 → 最近使用 排序，结果行可直接★收藏，常用项稳定靠前。
- 中键菜单调整：「检查更新」移出中键菜单（主页版本图标仍是更新入口），widget 菜单项用括号标注来源。
- 取色菜单项改为由颜色 widget 自己注册（与其它 widget 功能统一），不再是宿主导入的原生菜单项。
- 剪贴板记录支持**固定置顶**：重要记录点图钉固定，置顶显示且不被新内容挤出，重启后保留。
- 托盘右键菜单增强：新增**开机自启开关**（勾选反映系统实际注册状态）与**检查更新**，与显示/隐藏、退出并排。
- widget 搜索与词典搜索合并为统一搜索框组件（SearchBox），行为经 props 参数化；修复词典搜索建议点击失效的问题。
- 为主页入场、主题切换、天气图标、待办/剪贴板列表、倒计时秒数、按钮按压等补充克制的微动效。

## [0.1.0] - 2026-08-13

### 新增
- 自动检测 GitHub Releases 新版本：启动时后台检查，发现更新可一键下载并安装。
- 新增 `CHANGELOG.md` 版本变化记录。
- GitHub CI：PR / push 自动跑类型检查、Rust 测试与 clippy；打 `v*` tag 自动构建 Windows 安装包并发布 Release。修复两处既有 clippy 警告。
